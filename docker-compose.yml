web:
  command: uwsgi --http :8000 --wsgi-file project/wsgi.py --static-map /referrals/static=/app/project/static
  build: .
  volumes:
    - .:/app
  links:
    - db
  ports:
    - "8001:8000"
  env_file:
    - .env
db:
  image: dangerfarms/postgis:latest
